// Backend configuration
export const BACKEND_URL = "http://localhost:4000"; // Change to deployed backend later

// Feature flags
export const ENABLE_BACKEND = false; // Set to false if backend is not running
export const DEBUG_MODE = true; // Enable console logging for debugging

// Network configuration
export const DEFAULT_NETWORK = "mainnet"; // or "testnet", "localhost"
export const SUPPORTED_NETWORKS = ["mainnet", "testnet", "localhost"];
